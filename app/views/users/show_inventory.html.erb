<section class="user">
  <%= render 'show_user' %>
</section>

<%= render 'shared/flash' %>
<section class="user-list">
  <div class="subheader">
    <div class="main-title"><%= @title %></div>
  </div>

  <% if @inventories.any? %>
    <%= render 'shared/food_inventory' %>
  <% else %>
    <p class="notthing"><%= "保存中の食材はありません。" %></p>
  <% end %>
  <%= paginate @inventories %>

  <div class="submit"><%= link_to '食材を登録', new_inventory_path, class:"btn slide-bg" %></div>

</section>
