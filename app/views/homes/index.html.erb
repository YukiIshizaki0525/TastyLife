<div id="main-content">
  <main>
    <section class="introduction">
      <div class="introduction__inner">
        <div class="introduction__img">
          <%= image_tag 'introduction.jpg' %>
        </div>
        <div class="introduction__texts">
          <div class="introduction__texts-inner">
            <div class="introduction__title main-title">
              Share recipes!
            </div>
            <div class="introduction__sub sub-title">
              レシピを共有しよう
            </div>
            <div class="introduction__description">
            <p>レシピの投稿や閲覧が可能で<br>簡単にシェアできるアプリです。</div></p>
            <p>あなたの望むレシピがきっと見つかります。</p>
            </div>
          </div>
        </div>
      </div>
    </section>
    <section class="newarrival">
      <div class="subheader">
        <div class="main-title">New Arrival</div>
        <div class="sub-title">新しいレシピを見つけましょう</div>
      </div>
      <div class="newarrival__container">
        <% @newrecipes.each do |recipe| %>
          <%= link_to(recipe_path(id: recipe.id), class:'newarrival__item') do %>
            <div class="newarrival__img">
              <%= image_tag(recipe.image) if recipe.image.attached? %>
            </div>
            <div class="newarrival__detail">
              <span class="timestamp"><%= recipe.created_at.strftime('%Y年%m月%d日') %></span><br>
              <p class="newarrival__recipetitle"><%= recipe.title %></p><br>
              <p class="newarrival__description"><%= recipe.description %></p>
            </div>
          <% end %>
        <% end %>
        </div>
      </div>
    </section>
    
    <section class="recipe">
      <div class="subheader">
        <div class="main-title">Recipe List</div>
        <div class="sub-title">フォロー中のレシピ</div>
      </div>
      <div class="recipe__container">
        <% @recipes.each do |recipe| %>
          <div class="recipe__item">
            <%= link_to(recipe_path(id: recipe.id), class:'recipe__img') do %>
              <%= image_tag(recipe.image) if recipe.image.attached? %>
            <% end %>
            <div class="recipe__detail">
              <span class="timestamp"><%= recipe.created_at.strftime('%Y年%m月%d日') %></span><br>
              <%= link_to recipe.title, recipe_path(id: recipe.id), class: 'recipe__title' %>
              <div class="recipe__description"><%= recipe.description %></div>
              <%= link_to(user_path(id: recipe.user_id), class:'recipe__userinfo') do %>
                <div class="recipe__useravatar"><%= image_tag(recipe.user.avatar) %></div>
                <p class="recipe__username"><%= recipe.user.name %></p>
              <% end %>
            </div>
          </div>
        <% end %>
      </div>
    </section>
  </main>
</div>
