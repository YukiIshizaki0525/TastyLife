<header>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <%= link_to "RecipeApp", root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%= header_link_item('ホーム', root_path) %>
        </li>
        <li class="nav-item">
          <%= header_link_item('レシピ一覧', recipes_path) %>
        </li>
        <li class="nav-item">
          <% if user_signed_in? %>
            <li>
              <%= header_link_item('アカウント編集', edit_user_registration_path) %>
            </li>
            <li>
              <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: 'nav-link' %>
            </li>
          <% else %>
            <li>
              <%= header_link_item('新規登録', new_user_registration_path) %>
            </li>
            <li>
              <%= header_link_item('ログイン', new_user_session_path) %>
            </li>
          <% end %>
        </li>
      </ul>
    </div>
  </nav>
</header>