<span>
  <% if recipe.image.attached? %>
    <%= link_to image_tag(recipe.display_image, class: 'img-thumbnail recipe-detail col-lg-4 col-sm-6 mb-3'), recipe_path(id: recipe.id) %>
  <% else %>
    <%= link_to image_tag('default_user.png', :size => "200x200" ), user_path(id: user.id) %>
  <% end %>
  <%= link_to image_tag(recipe.display_image, class: 'img-thumbnail recipe-detail col-lg-4 col-sm-6 mb-3'), recipe_path(id: recipe.id) if recipe.image.attached? %>
</span>